0x00 什么是K-Means
     K-Means是K均值

0x01 K-Means作用
     是无监督学习的聚类算法

0x02 K-Means算法实现的流程
     step1：随机初始化K个质心，
     step2：计算样本空间中所有点距离每一个质心的位置,距离哪一个质心距离更近，就把这个点聚到质心所在的那一类，
     step3：第一轮聚类得到不同的类以后，将各个类中所有点坐标的均值（为什么要用均值：因为均值是那一类点最具代表性特征所在的点），将这个新的坐标点作为质心的更新，
     step4：不断重复上述操作，直到质心的点位置不在发生改变或改变的范围在阈值范围之内，就认为算法收敛。

0x03 K-Means算法的python实现
